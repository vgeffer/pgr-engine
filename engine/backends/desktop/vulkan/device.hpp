#pragma once
#include "vulkan/vulkan.hpp"
#include <vulkan/vulkan.hpp>
#include <cassert>

namespace utils::vulkan {

    class device { 

       /* public:
            device(); /* Create placeholder device

            ~device();

            void init();
            
            operator vk::PhysicalDevice() const noexcept { assert(m_is_valid); return m_phys_device; }

        private:

        private:
            bool               m_is_valid;                                       
            vk::Device         m_device;

            */

    };
}